# Python
__pycache__/
*.pyc
.pytest_cache/

# Environments / credentials
.venv/
.env
.env.*
!.env.example

# Editor / tool caches
.claude/
.cache/

# Training / experiment artifacts
wandb/
artifacts/
artifacts_*/
megatron_checkpoints/
nemo_experiments/
tea_debug.log

# Eval / experiment outputs
results/

# Internal notes / write-ups
docs/

# Ad-hoc experiments / EDA scripts + outputs
exp/

# Dataset splits — regenerated by scripts in src/ ; track via dataset registry
/data/
src/evals/product_matching/data/
src/evals/product_matching/system_prompt.txt

# demo/ runtime artifacts (scraper cache + debug DOM dumps + fanout HTMLs)
demo/cache/

# Stray dir from unexpanded $OUTPUT_DIR in eval-factory yaml template
/$OUTPUT_DIR/
